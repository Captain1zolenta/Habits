import QtQuick
import QtQuick.Controls
import QtQuick.Layouts

Rectangle {
    id: root
    color: "#0a0a0a"

    // Здесь будет весь ваш UI
    Text {
        text: "Habits - Главная страница"
        color: "#ffffff"
        font.pixelSize: 24
        anchors.centerIn: parent
    }
}
